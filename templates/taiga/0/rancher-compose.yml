.catalog:
  name: "Taiga"
  version: "v0.1-gerencio"
  description: "Project management platform for agile developers"
  uuid: taiga-0
  minimum_rancher_version: v0.51.0
  questions:
    - variable: "HOSTNAME"
      label: "Hostname used by taiga"
      description: "Hostname used by taiga"
      required: true
      type: "string"
    - variable: "SECRET_KEY"
      label: "Secret key"
      description: "Secret key used by taiga api"
      required: true
      type: "string"
    - variable: "DEBUG"
      label: "Enable Debug?"
      description: "Enable Debug?"
      required: true
      type: "string"
      default: "False"
    - variable: "TEMPLATE_DEBUG"
      label: "Enable Debug on Template ?"
      description: "Enable Debug on Template ?"
      required: true
      type: "string"
      default: "False"
    - variable: "PUBLIC_REGISTER_ENABLED"
      label: "Enable public subscriptions?"
      description: "Enable public subscriptions?"
      required: true
      type: "string"
      default: "False"
    - variable: "SCHEME"
      label: "Scheme (http or https)"
      description: "Scheme (http or https)"
      required: true
      type: "string"
      default: "http"
    - variable: "EMAIL_USE_TLS"
      label: "Use TLS on email?"
      description: "Use TLS on email?"
      required: true
      type: "string"
      default: "False"
    - variable: "EMAIL_USE_SSL"
      label: "Use ssl on email?"
      description: "Use ssl on email?"
      required: true
      type: "string"
      default: "True"
    - variable: "EMAIL_HOST"
      label: "Email host"
      description: "Email Host"
      required: true
      type: "string"
    - variable: "EMAIL_PORT"
      label: "Email port"
      description: "Email Port"
      required: true
      type: "string"
    - variable: "EMAIL_HOST_USER"
      label: "Email host User"
      description: "Email host User"
      required: true
      type: "string"
    - variable: "EMAIL_HOST_PASSWORD"
      label: "Email password"
      description: "Email password"
      required: true
      type: "string"
    - variable: "DEFAULT_FROM_EMAIL"
      label: "Default from email"
      description: "Default from email"
      required: true
      type: "string"
    - variable: "POSTGRES_USER"
      label: "postgres user"
      description: "postgres user"
      required: true
      type: "string"
      default: "taiga"
    - variable: "POSTGRES_PASSWORD"
      label: "postgres password"
      description: "postgres password"
      required: true
      type: "string"
      default: "taiga"
    - variable: POSTGRESQL_DATA
      label: "postgres volume for mount"
      description: "postgres volume for mount"
      required: true
      type: "string"
      default: "/home/core/dockerdata/taiga-back"
    - variable: "TAIGA_GITHUB_CLIENT_ID"
      label: "Taiga github id for integration"
      description: "Taiga github id for integration"
      required: true
      type: "string"
    - variable: "TAIGA_GITHUB_CLIENT_SECRET"
      label: "Taiga github secret for integration"
      description: "Taiga github secret for integration"
      required: true
      type: "string"
    - variable: TAIGA_BACK_DATA
      label: "Taiga volume for mount"
      description: "Taiga volume for mount"
      required: true
      type: "string"
      default: "/home/core/dockerdata/taiga-back"
taiga-back:
taiga-back:








